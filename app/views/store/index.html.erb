<div class="container">
  <h1>My Store</h1>
  <%= link_to 'My Shop', 'products', :class => 'btn btn-primary' %>
</div>

<!-- <div class="container">
  <h1>Products</h1>
  <%# if user_signed_in? && current_user.has_role?(:seller) %>
    <%=# link_to "New product", new_product_path, :class => 'btn btn-primary mb-3' %>
  <%# end %>
  <div class="col-md-12 col-md-offset-12">
    <table class="table">
      <thead>
        <tr>
          <th>Title</th>
          <th>Description</th>
          <th>Price</th>
          <th>Image</th>
        </tr>
      </thead>
      <tbody>
        <%# @products.each do |product| %>
          <tr>
            <td><%= product.title %></td>
            <td><%= product.description %></td>
            <td><span>$</span> <%= product.price %></td>
            <td><%# product.images.each do |image| %>
                  <%=# image_tag image ,size: "30x30" ,:class => 'class="card-img-top"'%> 
                <%# end %>
            </td>
            <td>
              <%=# link_to 'Show', product_path(product), :class => 'btn btn-outline-success' %>
              <%# if user_signed_in? && current_user.has_role?(:seller) %>
                <%=# link_to 'Edit', edit_product_path(product), :class => 'btn btn-outline-info'  %>
                <%=# link_to 'Destroy', product_path(product),
                  #method: :delete,
                  #data: { confirm: 'Are you sure?' }, :class => 'btn btn-outline-warning'  %>
              <%# end %>
            </td>
            <td>
              <%=# form_for(:line_item, url: order_line_items_path(current_user.latest_order), local: true ) do |form| %>
                <%#= form.hidden_field :product_id, value: product.id %>
                <%#= form.number_field :quantity, value: 1, min: 1 %>
                <%#= form.submit 'Add to cart' %>
              <%# end %>
            </td>
          </tr>
        <% #end %>
      </tbody>
    </table>
  </div>
</div>



 -->
<!-- 
 <div class="container">
<h1>Products</h1>
  <%# if user_signed_in? && current_user.has_role?(:seller) %>
    <%#= link_to "New product", new_product_path, :class => 'btn btn-primary mb-3' %>
  <%# end %>
  <%# @products.each do |product| %>
    <div class="container">
      <div class="row"> 
        <div class="col-sm-12 col-md-4">
          <div class="card" style="width: 18rem;">
            <%# product.images.each do |image| %>
                <%#= image_tag image ,size: "286x180" ,:class => 'class="card-img-top"'%> 
            <%# end %>
            <div class="card-body">
              <h5 class="card-title"><%= product.title %></h5>
              <p class="card-text"><%= product.description %></p>
              <p><%#= link_to 'Show', product_path(product), :class => 'btn btn-outline-success' %>
              <%# if user_signed_in? && current_user.has_role?(:seller) %>
                <%#= link_to 'Edit', edit_product_path(product), :class => 'btn btn-outline-info'  %>
                <%#= link_to 'Destroy', product_path(product),
                method: :delete,
                data: { confirm: 'Are you sure?' }, :class => 'btn btn-outline-warning'  %>
              <% #end %></p>
              <p>
                <%#= form_for(:line_item, url: order_line_items_path(current_user.latest_order), local: true ) do |form| %>
                  <%#= form.hidden_field :product_id, value: product.id %>
                  <%#= form.number_field :quantity, value: 1, min: 1 %>
                  <%#= form.submit 'Add to cart', :class => 'mt-1' %>
                <% #end %>
              </p>
            </div>
          </div>
        </div>
      </div>  
    </div>
  <% #end %> -->
<!-- </div> -->



<!-- Main page product index without carts
<div class="container">
  <h1>Products</h1>
  <%# if user_signed_in? && current_user.has_role?(:seller) %>
    <%#= link_to "New product", new_product_path, :class => 'btn btn-primary mb-3' %>
  <%# end %>
  <div class="col-md-12 col-md-offset-12">
    <table class="table">
      <thead>
        <tr>
          <th>Title</th>
          <th>Description</th>
          <th>Price</th>
          <th>Image</th>
        </tr>
      </thead>
      <tbody>
        <%# @products.each do |product| %>
          <tr>
            <td><%= product.title %></td>
            <td><%= product.description %></td>
            <td><span>$</span> <%= product.price %></td>
            <td><%# product.images.each do |image| %>
                  <%=# image_tag image ,size: "30x30" ,:class => 'class="card-img-top"'%> 
                <%# end %>
            </td>
            <td>
              <%=# link_to 'Show', product_path(product), :class => 'btn btn-outline-success' %>
              <%# if user_signed_in? && current_user.has_role?(:seller) %>
                <%=# link_to 'Edit', edit_product_path(product), :class => 'btn btn-outline-info'  %>
                <%=# link_to 'Destroy', product_path(product),
                  #method: :delete,
                  #data: { confirm: 'Are you sure?' }, :class => 'btn btn-outline-danger'  %>
              <%# end %>
            </td>
            <td>  
              <%# if user_signed_in? %> 
                <%=# form_for(:line_item, url: order_line_items_path(current_user.latest_order), local: true ) do |form| %>
                <%=# form.hidden_field :product_id, value: product.id %>
                <td> 
                  <%=# form.number_field :quantity, value: 1, min: 1 %>
                </td>
                <td>
                  <%=# form.submit 'Add to cart' %>
                </td> 
              <%# end %>
              <%# else%>
                <td>
                  <%=# link_to 'Add to Cart', new_user_session_path, class: 'btn btn-primary' %>
                </td>
              <%# end %>
            </td>
          </tr>
        <%# end %>
      </tbody>
    </table>
  </div>
</div>

 -->